function add(a,b){
    return a + b;
}//using simple function syntax

const addthree = (a,b,c) => {
    return a + b + c;
}//using arrow function syntax

module.exports = addthree; //used the module.exports to export the module